name: Run Tests
on:
  pull_request:
    branches: 
      - 'main'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Test Code
        run: cargo test
        continue-on-error: false
    
